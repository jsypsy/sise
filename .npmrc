# Cloudflare Workers Builds는 빌드 시 NODE_ENV=production이라 devDependencies를
# 빠뜨린다. 빌드 도구(@opennextjs/cloudflare, wrangler, typescript, tailwind 등)가
# 필요하므로 dev 포함을 강제한다. (로컬 pnpm·Vercel엔 영향 없음)
include=dev
